import { Meta, Story, Preview, ArgsTable } from "@storybook/addon-docs/blocks";
import AppLoader from "./index";

<Meta title="Components/AppLoader" component={AppLoader} />

# AppLoader

The `AppLoader` component displays a loading indicator with an image, typically used while initializing the application.

<Preview>
  <Story name="Basic">
    <AppLoader imageSrc="https://bi.datawiz.io/static/images/logo.svg" />
  </Story>
</Preview>

## Props

<ArgsTable of={AppLoader} />
